<h1 class="info-box">Burgers!</h1>
<img src="./assets/img/kawaii-burger.png" alt="kawaii-burger">
<h2 class="info-box">Burgers that are not devoured!</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    <li>
        {{burger_name}}
    </li>
<br>
    <button class="change-devoured" data-id="{{id}}" data-newDevoured="{{devoured}}">EAT DA BURGER</button>
<br>
<br>
    {{/unless}}
    {{/each}}
    
</ul>

<h2 class="info-box">Burgers that are devoured!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    <li>
        {{burger_name}}
    </li>
<br>
    {{/if}}
    {{/each}}
</ul>

<h2 class="info-box">Add Your Own Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="newBurger">Burger Name:</label>
        <input type="text" size="50" id="newBurger" name="burger_name">
    </div>
<br>
<br>

    <div class="form-group">
        <label for="devour">Has this burger been devoured?</label>
        <br>
        <br>
        <input type="radio" name="devoured" value="1" > Devoured!
        <br>
        <br>
        <input type="radio" name="devoured" value="0"checked> Not yet Devoured!
        <br>
        <br>
    </div>

    <button type="submit">Add Burger</button>
</form>